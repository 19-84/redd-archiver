# Flask Search Server Dependencies
# Synced with pyproject.toml on 2026-06-09

# Web framework (pulls werkzeug>=3.1 which fixes CVEs)
Flask==3.1.3

# Production WSGI server (SECURITY: fixes HTTP request smuggling CVE)
gunicorn==26.0.0

# Rate limiting
Flask-Limiter==4.1.1

# CORS support (SECURITY: fixes private network header CVE)
Flask-CORS==6.0.2

# CSRF protection
Flask-WTF==1.2.2

# HTML escaping
MarkupSafe==3.0.3

# PostgreSQL adapter with binary wheels
psycopg[binary,pool]==3.3.3

# System monitoring (required by console_output.py)
psutil==7.2.2

# Fast JSON parsing (required by postgres_database.py)
# SECURITY: >=3.11.6 fixes a high-severity advisory (was pinned at 3.11.5)
orjson==3.11.8

# Template rendering (SECURITY: defense in depth)
Jinja2==3.1.6
